<section class=" md:pt-28 flex items-center md:block min-h-screen relative">



  {# Animated decorative circles #}
  <div class="bg-primary h-6 w-6 animate-bounce absolute right-12 top-24 rounded-full"></div>
  <div class="bg-primary h-6 w-6 animate-bounce absolute left-12 bottom-48 rounded-full"></div>

  <div class="relative max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 space-y-6 z-10">

    {# Badge #}
    <div class="flex justify-center gap-3 items-center">
      <span class="h-4 w-4 bg-primary rounded-full animate-ping"></span>
      <span class="px-3 py-1 bg-blue-600/10 text-primary rounded-full text-sm font-medium">
        Streamline Your Workflow
      </span>
    </div>

    {# Title #}
    <h1 class="text-4xl md:text-5xl text-center lg:text-6xl font-bold text-gray-900 leading-tight">
      Manage Tickets with
      <span class="text-primary">Ease</span>
    </h1>

    {# Description #}
    <p class="text-lg text-gray-600 max-w-lg text-center mx-auto">
      Ticko is the modern ticket management system designed for teams that want to work smarter,
      not harder. Track, prioritize, and resolve issues in real-time.
    </p>

    {# CTA Buttons #}
    <div class="flex flex-col md:flex-row gap-4 pt-4 justify-center">
      <a href="{{ base_path }}/auth/signup"
         class="text-center hover:bg-cyan-600 text-white bg-primary px-24 py-4 rounded-md transition-colors">
        Get Started Free
      </a>
      <a href="#features"
         class="text-center hover:bg-black/5 border-2 border-gray-300 text-black px-24 py-4 rounded-md transition-colors">
        Learn More
      </a>
    </div>

    {# Footer text #}
    <p class="text-sm text-gray-600 text-center">
      No credit card required · Start managing tickets in minutes.
    </p>
  </div>

  {# Wave decoration #}
  <div class="absolute bottom-0 left-0 right-0 w-full overflow-hidden">
    <img class="w-full min-w-[1440px] hidden md:block"
         src="{{ base_path }}/public/assets/wave.svg"
         alt="Wave decoration"/>
    <img class="w-full min-w-full block md:hidden"
         src="{{ base_path }}/public/assets/wave-mobile.svg"
         alt="Wave decoration"/>
  </div>
</section>
